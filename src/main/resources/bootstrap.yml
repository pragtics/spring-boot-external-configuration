spring:
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/pragtics/spring-boot-external-configuration.git
          search-paths: configuration
